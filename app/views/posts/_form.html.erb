<div id="post_form">
    <%= form_for(@post) do |f| %>
      
      <% if @post.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

          <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <div class="field">
        <%= f.label :title %><br>
        <%= f.text_field :title, :autofocus => true, :required => true %>
      </div>
      <div class="field">
        <%= f.label :post %><br>
        <%= f.cktext_area :desc %>
      </div>

      <div class="actions">
        <%= f.submit :class => "btn btn-primary btn-md" %><br />
      </div>
    <% end %>
</div>

